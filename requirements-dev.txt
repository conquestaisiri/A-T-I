# ATI — Development profile
# -----------------------------------------------
# Test, lint, and type-check tooling.  Installs the core runtime via -r.
-r requirements.txt
pytest>=7.4.0
pytest-asyncio>=0.21.0
mypy>=1.5.0
types-PyYAML>=6.0.12
ruff>=0.15.0
