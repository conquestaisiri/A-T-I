# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Environment
.env
.venv
venv/
ENV/
env.bak/
venv.bak/

# IDEs
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Pytest
.pytest_cache/
.ruff_cache/
.mypy_cache/
coverage.xml
htmlcov/
.coverage
.coverage.*

# Research reference clones at the repo root (extracted lessons live in
# experiments/). Anchored: `backend/domain/research/` and
# `backend/application/research/` are tracked code and must never match.
/research/

# Local SQLite persistence (ADR 0004)
data/
*.db
*.sqlite

# Secrets — never commit (mirrors .dockerignore)
.env.local
.env.*.local
api_keys.env
**/api_keys.env
keys.env
**/keys.env
SagaxAI-API-Keys/
reports/

# Gitleaks / secret scanning
.gitleaks.toml

# Runtime logs and throwaway artifacts
*.log
shot_*.png
dashboard_shot.png

# Personal/local-only material
Old Chat With Opencode/
