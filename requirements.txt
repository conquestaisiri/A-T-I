# ATI — Core runtime profile
# -----------------------------------------------
# Enough to run: observation -> context -> rule-based decision -> paper execution.
# Heavy optional integrations (AI, ML, research, venues) live in profile-specific files.
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
websockets>=11.0.3
pyyaml>=6.0.0
httpx>=0.27.0
numpy>=1.26.0
