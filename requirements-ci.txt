# ATI — CI dependency set (direct imports only; Linux-resolvable)
# Derived from top-level imports in backend/ + tests/; versions floored at
# the locally-verified majors. Transitive resolution is left to pip.
fastapi>=0.100
uvicorn[standard]>=0.23
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
websockets>=11
PyYAML>=6.0
httpx>=0.27
aiohttp>=3.9
feedparser>=6.0
numpy>=1.26
edgartools==5.46.0
# Typed heavy stack — kept identical to the local dev env so inline
# type: ignore usage in portfolio_risk/edgar paths behaves the same on CI.
pandas==3.0.5
riskfolio-lib==7.3.0
cvxpy==1.9.2
pydantic-ai-slim>=2.0
# pydantic-ai optional provider extra required by reasoner tests at collection.
openai==2.53.0

# Tooling + typing stubs
pytest>=7.4
pytest-asyncio>=0.21
mypy>=1.5
ruff>=0.15
types-PyYAML>=6.0